[build]
builder = "dockerfile"
# Production Dockerfile that compiles and runs both API and Web
dockerfilePath = "../Dockerfile.production"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
