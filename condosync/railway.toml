[build]
builder = "dockerfile"
# Simple Dockerfile that compiles and runs both API and Web
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
