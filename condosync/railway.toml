[build]
builder = "dockerfile"
# Web-only Dockerfile: serves React SPA via Nginx
# API runs separately on Railway
dockerfilePath = "Dockerfile.web.final"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
