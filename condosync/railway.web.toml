[build]
builder = "dockerfile"
# Contexto de build = condosync/ — Dockerfile canônico do Web
dockerfilePath = "apps/web/Dockerfile"

[deploy]
healthcheckPath = "/"
healthcheckTimeout = 60
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
