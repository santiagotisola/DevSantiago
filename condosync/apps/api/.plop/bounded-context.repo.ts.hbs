/**
 * {{pascalCase name}}Repository — única importação de Prisma neste sub-context.
 *
 * Gerado por `npx plop bounded-context {{name}}`.
 * Edite as interfaces para refletir o domínio real.
 */
import { prisma } from "../../../../config/prisma";
import type { PrismaTx } from "../types";

export interface {{pascalCase name}}Repository {
  // TODO: declare contratos (findById, list, create, update).
  findById(id: string, tx?: PrismaTx): Promise<unknown | null>;
}

export class Prisma{{pascalCase name}}Repository implements {{pascalCase name}}Repository {
  private client(tx?: PrismaTx) {
    return tx ?? prisma;
  }

  async findById(_id: string, _tx?: PrismaTx) {
    // TODO: implementar
    throw new Error("not implemented");
  }
}

export const {{camelCase name}}Repository: {{pascalCase name}}Repository =
  new Prisma{{pascalCase name}}Repository();
